---
title: API Reference
description: Base URLs, authentication, error handling, pagination, and conventions for the Podium REST API
---

## Base URLs

| Environment | Base URL |
|------------|----------|
| **Production** | `https://api.podium.build/api/v1` |
| **Staging** | `https://podium-staging.up.railway.app/api/v1` |
| **Local** | `http://localhost:3030/api/v1` |

All endpoints are prefixed with `/api/v1`. The interactive API playground is available at [api.podium.build/docs](https://api.podium.build/docs).

## Authentication

Most endpoints require a Bearer token:

```bash
curl -X GET https://api.podium.build/api/v1/products \
  -H "Authorization: Bearer podium_live_your_key_here"
```

Keys prefixed with `podium_test_` route to staging; `podium_live_` routes to production. See [Authentication](/platform/authentication) for details on key management, subscription tiers, and rate limiting.

### Public Endpoints

Some endpoints require no authentication:

- `/agentic/*`. AI agent product discovery and checkout
- `/webhooks/*`. Inbound webhook handlers
- `/solver/*`. Solver discovery for Task Pool

## Request Format

All request bodies use JSON with `Content-Type: application/json`. Every endpoint validates its input with Zod schemas. Invalid requests return a 422 with field-level error details.

```bash
curl -X POST https://api.podium.build/api/v1/product \
  -H "Authorization: Bearer podium_live_..." \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Organic Face Serum",
    "price": 2500,
    "currency": "USD",
    "description": "Lightweight hydrating serum"
  }'
```

## Response Format

Successful responses return JSON. Single resources return the object directly; collections return arrays. All responses include standard HTTP status codes.

### Success Responses

| Status | Meaning |
|--------|---------|
| `200` | Success. Resource returned |
| `201` | Created. New resource |
| `204` | No content. Successful deletion |

### Error Responses

Errors return a consistent shape:

```json
{
  "status": 422,
  "error": "Validation failed",
  "details": [
    {
      "field": "price",
      "message": "Expected number, received string"
    }
  ]
}
```

| Status | Meaning |
|--------|---------|
| `400` | Bad request. Malformed input |
| `401` | Unauthorized. Missing or invalid API key |
| `402` | Payment required. X402 crypto payment needed |
| `403` | Forbidden. Insufficient subscription tier or role |
| `404` | Not found |
| `409` | Conflict. Duplicate resource |
| `422` | Validation error. Request body failed schema validation |
| `429` | Rate limited. Retry after the indicated delay |
| `500` | Internal server error |

## Pagination

Collection endpoints support offset-based pagination:

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `limit` | integer | 20 | Items per page (1-100) |
| `offset` | integer | 0 | Number of items to skip |

```bash
GET /api/v1/products?limit=10&offset=20
```

## IDs

Podium uses [CUID2](https://github.com/paralleldrive/cuid2) for all entity IDs. These are collision-resistant, URL-safe, and sortable by creation time.

```
clxyz1234567890abcdef  # Example CUID2
```

## Recent Additions

| Surface | Endpoint or change | Description |
|---|---|---|
| **Universal Checkout** | `POST /checkout-intents`, `GET /checkout-intents/:id`, `POST /checkout-intents/:id/confirm`, `GET /checkout-intents/:id/payment-config` | Two-phase checkout intent for supported merchant URLs. See [Universal Checkout](/agentic/universal-checkout) and the [Checkout Intents API](/api-reference/checkout-intents). |
| **Universal Checkout** | `POST /products/lookup` | Resolve any merchant URL into a canonical product record. |
| **Merchant Intelligence** | (platform feature, no public endpoint) | A platform-wide registry that classifies merchant storefronts and automatically expands checkout coverage. See [Merchant Intelligence](/platform/merchant-intelligence). |
| **In-Chat Buyability** | `buyableInChat` on companion `ProductCard` surfaces | **Server-derived. Do not re-derive on the client.** Mirrors live adapter selection. See [Product Feed](/agentic/product-feed#buyableinchat) for the full contract. |
| **Guest Sessions** | `POST /companion/guest/session`, `POST /companion/guest/convert` | Try-before-sign-up flow with merge-on-convert. See [Guest Experience](/agentic/guest-experience). |
| **World Sign-In Nonce** | `POST /companion/world/nonce` | Server-issued single-use SIWE nonce for World wallet authentication. |
| **Companion chat** | `GET /companion/agent/chat/history/:userId` | Retrieve full chat history for a user's agent conversation. |
| **Companion creators** | `GET /companion/creator/:handle/products`, `POST /companion/profile/:userId/seed-from-creator` | Browse creator catalogs and seed user profiles from a creator's taste graph. |
| **Subscriptions** | `POST /companion/subscription/checkout`, `POST /companion/subscription/portal`, `GET /companion/subscription/:userId/status` | Stripe-powered subscription billing for companion agents. |

### SSE Events

The conversational agent endpoint (`POST /companion/agent/chat/stream`) streams Server-Sent Events. The full set of event types is documented in [Conversational Agent → Event Types](/agentic/conversational-agent#event-types). Recently added event types:

| Event | Description |
|-------|-------------|
| `checkout_intent_created` | The agent created a Universal Checkout intent from a merchant URL. Payload mirrors the `POST /checkout-intents` response. |
| `quiz_requested` | The agent summoned an inline profile quiz. Payload is an `InlineQuizDefinition`. |
| `memory_queued` | A preference has been extracted and queued for memory storage. |
| `confirmation_required` | The agent is asking the user to confirm before proceeding (e.g., high-value purchase). |
| `spend_limit_exceeded` | The requested action exceeds the user's configured spend limit. |

## Endpoint Groups

The API is organized into these groups:

<CardGroup cols={2}>
  <Card title="Products" icon="box" href="/api-reference/products">
    Product CRUD, attributes, media, categories
  </Card>
  <Card title="Creators" icon="store" href="/api-reference/creators">
    Creator profiles, storefronts, orders, payouts
  </Card>
  <Card title="Users" icon="user" href="/api-reference/users">
    User profiles, wallets, following, notifications
  </Card>
  <Card title="Orders" icon="receipt" href="/api-reference/orders">
    Order lifecycle, checkout, shipping
  </Card>
  <Card title="Payments" icon="credit-card" href="/api-reference/payments">
    Stripe, x402, Coinbase, embedded wallet
  </Card>
  <Card title="Points" icon="coins" href="/api-reference/points">
    Points ledger, earning, spending, history
  </Card>
  <Card title="Campaigns" icon="bullhorn" href="/api-reference/campaigns">
    Swipe, vote, survey, UGC campaigns
  </Card>
  <Card title="Rewards & Airdrops" icon="gem" href="/api-reference/nfts">
    On-chain rewards, minting, airdrops, redemption
  </Card>
  <Card title="Companion" icon="robot" href="/api-reference/companion">
    Intent profiles, interactions, recommendations
  </Card>
  <Card title="Task Pool" icon="list-check" href="/api-reference/task-pool-api">
    Tasks, pools, solvers, verification
  </Card>
  <Card title="Search" icon="magnifying-glass" href="/api-reference/search">
    Full-text search, autocomplete, discovery
  </Card>
  <Card title="Webhooks" icon="bell" href="/api-reference/webhooks">
    Stripe, Shopify, Privy, internal events
  </Card>
</CardGroup>

<Note>
  Stuck? Email us at [team@podium.build](mailto:team@podium.build). We reply within a few hours.
</Note>
